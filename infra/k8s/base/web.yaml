apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracker-web
  namespace: tracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracker-web
  template:
    metadata:
      labels:
        app: tracker-web
    spec:
      containers:
        - name: web
          image: harbor.example.com/tracker/tracker-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: tracker-web
  namespace: tracker
spec:
  selector:
    app: tracker-web
  ports:
    - port: 3001
